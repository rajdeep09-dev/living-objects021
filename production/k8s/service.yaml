apiVersion: v1
kind: Service
metadata:
  name: living-objects-api
  labels:
    app.kubernetes.io/name: living-objects-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: living-objects-api
  ports:
    - name: http
      port: 8000
      targetPort: http

