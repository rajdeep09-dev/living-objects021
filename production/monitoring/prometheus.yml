global:
  scrape_interval: 15s
  evaluation_interval: 15s
rule_files:
  - /etc/prometheus/alerts.yaml
scrape_configs:
  - job_name: living-objects-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]

